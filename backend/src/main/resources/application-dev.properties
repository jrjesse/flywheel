# Dev profile — local defaults only, never use in production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.kafka.consumer.properties.spring.json.trusted.packages=com.antigravity.sales

app.security.jwt.secret=dev-jwt-secret-key-minimum-32-chars-long!!
crypto.secret.key=0123456789abcdef0123456789abcdef

spring.datasource.password=password

stripe.api.key=sk_test_placeholder
stripe.webhook.secret=whsec_placeholder

logging.level.org.springframework.mail=DEBUG

app.security.password-reset.expose-token=true
